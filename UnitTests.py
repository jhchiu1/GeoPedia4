from unittest import TestCase
import twitterapi

	def test_valid_date(self)
	#tests if rice is a valid date and asserts the answer is no
		twitterapi.valid_date('rice')
		self.assertEqual("Not a valid date: '{0}'.")
		self.assertRaises(argparse.ArgumentTypeError(msg))
